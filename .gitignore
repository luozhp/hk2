# 依赖与构建产物
node_modules/
dist/
*.tsbuildinfo

# 运行时数据（SQLite 数据库、自动备份、旧版 JSON 归档）
hunter/server/data/
!hunter/server/data/.gitkeep

# 环境变量（含凭证，禁止提交；保留 .env.example）
.env
.env.local
hunter/server/.env
hunter/server/.env.local

# 日志
*.log
