{
  "name": "hunter-b2b",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "dev": "concurrently -n server,web -c blue,green \"npm --prefix server run start:dev\" \"npm --prefix web run dev\"",
    "build": "npm --prefix server run build && npm --prefix web run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
